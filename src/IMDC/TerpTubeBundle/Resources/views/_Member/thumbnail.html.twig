{#
included by:
_Group:view.html.twig
_Member:index.html.twig
#}

{% trans_default_domain 'IMDCTerpTubeBundle' %}

{% if isSelection is not defined %}
    {% set isSelection = false %}
{% endif %}

{% for member in members %}
    {% if loop.index % 4 == 1 %}
        <div class="row">
            <ul class="list-unstyled">
    {% endif %}
    <li>
        <div class="col-md-3">
            <div class="thumbnail text-center">
                <div class="row">
                    <div class="col-md-4">
                        {% include 'IMDCTerpTubeBundle:_Member:avatar.html.twig' %}
                    </div>
                    <div class="col-md-8">
                        <h3>{% include 'IMDCTerpTubeBundle:_Member:username.html.twig' %}</h3>
                        <ul class="list-unstyled">
                            <li class="text-muted"><strong>{{ 'member.infoBlock.joined'|trans }}:</strong> {{ member.getJoinDate|date('M j Y') }}</li>
                            <li class="text-muted"><strong>{{ 'member.infoBlock.postCount'|trans }}:</strong> {{ member.getPostCount }}</li>
                        </ul>
                    </div>
                </div>
                <div class="row">
                    <div class="col-md-12" style="margin-top: 10px; margin-bottom: 10px">
                        {% if not isSelection %}
                            {% include 'IMDCTerpTubeBundle:_Member:connectTools.html.twig' with {'faSize': 'fa-2x'} %}
                        {% else %}
                            <button class="btn btn-default group-member" data-toggle="button" data-select="Select" data-selected="Selected" data-uid="{{ member.getId }}"></button>
                        {% endif %}
                    </div>
                </div>
            </div>
        </div>
    </li>
    {% if loop.index % 4 == 0 or loop.last %}
            </ul>
        </div>
    {% endif %}
{% endfor %}
