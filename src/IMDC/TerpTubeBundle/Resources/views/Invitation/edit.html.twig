{% extends 'IMDCTerpTubeBundle:Default:basetemplate.html.twig' %}

{% block rightsidecontent%}
<div class="span11">
    <h1>Invitation edit</h1>

    {{ form(edit_form) }}

    <ul class="unstyled inline record_actions">
        <li>
            <a class="btn" href="{{ path('invitation') }}">
                Back to the list
            </a>
        </li>
        <li>{{ form(delete_form) }}</li>
    </ul>
</div>
{% endblock %}
