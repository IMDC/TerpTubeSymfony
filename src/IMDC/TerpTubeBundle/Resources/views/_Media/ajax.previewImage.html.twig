{% extends 'IMDCTerpTubeBundle:_Media:preview.html.twig' %}

{% trans_default_domain 'IMDCTerpTubeBundle' %}

{% block preview %}
    <script type="text/javascript">
        $(document).ready(function() {
            $('.tt-media-preview img').on("click", function(e) {
                e.preventDefault();

                var element = $(this).parent();
                $tt.Core.Helper.toggleFullScreen(element);
            });

            $(document).on('fullscreenchange webkitfullscreenchange mozfullscreenchange MSFullscreenChange', function(e) {
                var element = $('.tt-media-preview img').parent();
                if ($tt.Core.Helper.isFullscreen()) {
                    element.addClass('tt-media-preview-fullscreen');
                } else {
                    element.removeClass('tt-media-preview-fullscreen');
                }
            });
        });
    </script>
{% endblock preview %}
