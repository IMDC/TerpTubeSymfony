{#media}
    <li data-mid="{data.id}">
        <div>
            {?data.thumbnail_path}<img src="{@generateUrl path=data.thumbnail_path /}" />{/data.thumbnail_path}
        </div> {! important !}
    </li>
{/media}
